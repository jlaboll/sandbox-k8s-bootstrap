containerd:
  version: 1.6.9
  config_dir: "/etc/containerd"

cni:
  binary_dir: "/opt/cni/bin/"
  config_dir: "/etc/cni/net.d/"

k8s: 
  version: 1.25.3-00
  gpg_key_url: "https://packages.cloud.google.com/apt/doc/apt-key.gpg"
  deb_repo_url: "https://apt.kubernetes.io/"
  deb_dist: "kubernetes-xenial"
  deb_branch: "main"

